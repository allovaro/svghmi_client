import LoginComponent from '../loginComponent/loginComponent';

const Login = (props) => {
    return (
        <LoginComponent
            mode={props.mode}
        />
    )
}

export default Login;
